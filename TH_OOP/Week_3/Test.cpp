#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    // char* s = new char[20];
    // cin.getline(s, 20);
    // char* t = new char[strlen(s) + 1];
    // for(int i=0; i<strlen(s); i++)
    // {
    //     t[i] = s[i];
    // }
    // t[strlen(s)] = '\0';

    // char* u = new char[3];
    // cin.getline(u, 50);
    // cout << u;

    int x = 232342123;
    int y = 2322;
    cout << x * y;
}